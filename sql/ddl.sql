CREATE TABLE students (
    id int,
    roll_no varchar(10),
    first_name varchar(100),
    last_name varchar(100),
	gender varchar(10),
    admission_year date,
    date_of_birth date,
    email varchar(100),
    phone_number int
);